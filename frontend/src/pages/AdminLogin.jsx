import { useState } from 'react';import { Eye, EyeOff, LoaderCircle, MoveLeft } from 'lucide-react';import { Link, Navigate, useLocation, useNavigate } from 'react-router-dom';import Brand from '../components/Brand';import { useAuth } from '../context/AuthContext';
export default function AdminLogin(){const [show,setShow]=useState(false);const [form,setForm]=useState({email:'',password:''});const [busy,setBusy]=useState(false);const [error,setError]=useState('');const {admin,loading,login}=useAuth();const navigate=useNavigate();const location=useLocation();if(!loading&&admin)return <Navigate to="/admin" replace/>;const submit=async(e)=>{e.preventDefault();setBusy(true);setError('');try{await login(form);navigate(location.state?.from?.pathname||'/admin',{replace:true})}catch(err){setError(err.response?.status===429?'Too many login attempts. Please try again later.':'Invalid email or password.')}finally{setBusy(false)}};return <main translate="no" className="notranslate grid min-h-screen place-items-center bg-ink p-5 text-white"><div className="w-full max-w-md"><Link to="/"><Brand/></Link><div className="panel mt-8 p-6 md:p-8"><p className="text-xs uppercase tracking-[.18em] text-ice">Private area</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.03em]">Admin access</h1><form onSubmit={submit} className="mt-8 space-y-5"><label><span className="label">Email</span><input required autoComplete="username" type="email" className="field" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label><span className="label">Password</span><span className="relative block"><input required autoComplete="current-password" type={show?'text':'password'} className="field pr-12" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 p-1 text-white/45" aria-label={show?'Hide password':'Show password'}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></span></label>{error&&<p role="alert" className="rounded-xl bg-danger/10 p-3 text-sm text-danger">{error}</p>}<button disabled={busy} className="btn btn-primary w-full">{busy?<LoaderCircle className="animate-spin" size={18}/>: 'Sign in'}</button></form></div><Link className="mt-6 flex items-center gap-2 text-sm text-white/45 hover:text-white" to="/"><MoveLeft size={16}/> Return to website</Link></div></main>}
