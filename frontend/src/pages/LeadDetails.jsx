import { useEffect,useState } from 'react';import { Check,Clipboard,ExternalLink,MoveLeft,Save,Trash2 } from 'lucide-react';import { Link,useNavigate,useParams } from 'react-router-dom';import toast from 'react-hot-toast';import api from '../lib/api';import { statuses,statusLabel } from '../constants';import StatusBadge from '../components/StatusBadge';
const Row=({label,children})=><div className="border-b border-white/[.05] py-4"><dt className="text-xs uppercase tracking-wider text-white/35">{label}</dt><dd className="mt-2 break-words text-sm text-white/75">{children||'—'}</dd></div>;
export default function LeadDetails(){const {id}=useParams();const navigate=useNavigate();const [lead,setLead]=useState(null);const [notes,setNotes]=useState('');const [error,setError]=useState(false);useEffect(()=>{api.get(`/admin/leads/${id}`).then(r=>{setLead(r.data.lead);setNotes(r.data.lead.adminNotes||'')}).catch(()=>setError(true))},[id]);const status=async(value)=>{try{const {data}=await api.patch(`/admin/leads/${id}/status`,{status:value});setLead(data.lead);toast.success('Status updated')}catch{toast.error('Could not update status')}};const save=async()=>{try{const {data}=await api.patch(`/admin/leads/${id}/notes`,{adminNotes:notes});setLead(data.lead);toast.success('Notes saved')}catch{toast.error('Could not save notes')}};const copy=async(value,label)=>{await navigator.clipboard.writeText(value);toast.success(`${label} copied`)};const remove=async()=>{if(!window.confirm(`Permanently delete ${lead.fullName}'s lead? This cannot be undone.`))return;try{await api.delete(`/admin/leads/${id}`);toast.success('Lead deleted');navigate('/admin/leads')}catch{toast.error('Could not delete lead')}};if(error)return <div><Link to="/admin/leads" className="text-ice">Back to leads</Link><p className="panel mt-5 p-6 text-danger">This lead could not be loaded.</p></div>;if(!lead)return <div className="h-96 animate-pulse rounded-2xl bg-surface"/>;return <><Link to="/admin/leads" className="inline-flex items-center gap-2 text-sm text-white/45 hover:text-white"><MoveLeft size={16}/> Back to leads</Link><div className="mt-6 flex flex-wrap items-center justify-between gap-4"><div><div className="flex items-center gap-3"><h1 className="text-3xl font-semibold">{lead.fullName}</h1><StatusBadge status={lead.status}/></div><p className="mt-2 text-sm text-white/40">Submitted {new Date(lead.submittedAt).toLocaleString()}</p></div><button onClick={remove} className="btn border border-danger/30 text-danger hover:bg-danger/10"><Trash2 size={17}/> Delete lead</button></div><div className="mt-7 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><section className="panel p-5 md:p-7"><h2 className="font-medium">Creator details</h2><dl className="mt-4 grid md:grid-cols-2 md:gap-x-8"><Row label="Full name">{lead.fullName}</Row><Row label="Email"><span className="flex items-center gap-2">{lead.email}<button onClick={()=>copy(lead.email,'Email')} title="Copy email"><Clipboard size={14}/></button></span></Row><Row label="Phone"><span className="flex items-center gap-2">{lead.phone||'—'}{lead.phone&&<button onClick={()=>copy(lead.phone,'Phone')} title="Copy phone"><Clipboard size={14}/></button>}</span></Row><Row label="Country / city">{lead.country}{lead.city?`, ${lead.city}`:''}</Row><Row label="Category">{lead.creatorCategory}</Row><Row label="Audience">{lead.audienceSize}</Row><Row label="Social platform">{lead.mainSocialPlatform}</Row><Row label="Social profile">{lead.socialProfileUrl?<a className="inline-flex items-center gap-1 text-ice" target="_blank" rel="noopener noreferrer" href={lead.socialProfileUrl}>Open profile <ExternalLink size={13}/></a>:'—'}</Row><Row label="Source">{lead.source}</Row><Row label="Referral code"><code className="text-ice">{lead.referralCode||'—'}</code></Row><Row label="Referred by">{lead.referredBy?<Link to={`/admin/leads/${lead.referredBy._id}`} className="text-ice hover:underline">{lead.referredBy.fullName} ({lead.referredBy.email})</Link>:'Direct / no referral'}</Row><Row label="Consent">{lead.consentGiven?<span className="inline-flex items-center gap-1 text-success"><Check size={14}/> Given{lead.consentAt?` · ${new Date(lead.consentAt).toLocaleString()}`:''}</span>:<span className="text-danger">Not recorded</span>}</Row><div className="md:col-span-2"><Row label="Creator description">{lead.creatorDescription}</Row></div></dl></section><aside className="space-y-6"><section className="panel p-5"><label><span className="label">Lead status</span><select className="field" value={lead.status} onChange={e=>status(e.target.value)}>{statuses.map(x=><option key={x} value={x}>{statusLabel(x)}</option>)}</select></label></section><section className="panel p-5"><label><span className="label">Internal admin notes</span><textarea className="field min-h-52 resize-y" maxLength={2000} value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Only admins can see these notes."/></label><div className="mt-3 flex items-center justify-between"><span className="text-xs text-white/30">{notes.length}/2000</span><button onClick={save} className="btn btn-primary"><Save size={16}/> Save notes</button></div></section></aside></div></>}
