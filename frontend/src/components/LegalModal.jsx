import { X } from 'lucide-react';
export default function LegalModal({ type, onClose }) { if (!type) return null; return <div className="fixed inset-0 z-[100] grid place-items-center bg-black/75 p-4" role="dialog" aria-modal="true" aria-labelledby="legal-title" onMouseDown={(e)=>e.target===e.currentTarget&&onClose()}><div className="panel max-w-xl p-7 shadow-2xl"><div className="flex items-center justify-between"><h2 id="legal-title" className="text-2xl font-semibold">{type}</h2><button onClick={onClose} aria-label="Close dialog" className="rounded-full p-2 hover:bg-white/10"><X size={20}/></button></div><p className="mt-6 leading-7 text-white/60">This {type.toLowerCase()} notice is placeholder content for pre-launch review. It does not constitute final legal terms and must be reviewed and replaced by qualified legal counsel before public launch.</p><p className="mt-4 text-sm text-white/40">Last placeholder revision: July 2026.</p></div></div>; }
